import { toolsInterface } from '../tools/el.js';
import Style from '../tools/style.js';
export declare const header: (appendTo: string, title: string, theme: Style) => toolsInterface;
export default header;
